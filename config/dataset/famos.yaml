defaults:
  - base_dataset
  - view_sampler: bounded

name: famos
data_root: datasets/famos

load_stereo: true
load_color: true
image_resize_factor: 1.0

input_image_shape: [256, 256]
original_image_shape: [1024, 1280]
cameras_are_circular: false

baseline_min: 1e-3
baseline_max: 1e10
max_fov: 100.0
